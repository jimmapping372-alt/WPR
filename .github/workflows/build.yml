name: WPR Android Smart Build
on: workflow_dispatch

jobs:
  build-android:
    runs-on: windows-latest
    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4
      with:
        submodules: 'recursive'

    - name: Setup .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Install Android MAUI Workload
      run: dotnet workload install android

    - name: Global NuGet Restore
      run: dotnet restore Src/WPR.sln
      # Βήμα 1: Προετοιμάζει όλες τις κοινές βιβλιοθήκες (XNA, Avalonia κ.λπ.)

    - name: Target Android Restore
      run: dotnet restore Src/UI/WPR.UI.Android/WPR.UI.Android.csproj /p:TargetFramework=net8.0-android
      # Βήμα 2: Επιβάλλει τη δημιουργία του target 'net8.0-android' που έλειπε

    - name: Publish Android APK
      run: dotnet publish Src/UI/WPR.UI.Android/WPR.UI.Android.csproj -c Release /p:TargetFramework=net8.0-android --no-restore /p:AndroidSigningKeyPass=dummy /p:AndroidSigningStorePass=dummy /p:ErrorOnMissingNativeLibs=false

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: WPR-Android-App
        path: Src/UI/WPR.UI.Android/bin/Release/net8.0-android/*-Signed.apk
        
